<%= form_with(model: rule, local: true) do |form| %>
  <% if rule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rule.errors.count, "error") %> prohibited this rule from being saved:</h2>

      <ul>
      <% rule.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :desc %>
    <%= form.text_field :desc %>
  </div>

  <div class="field">
    <%= form.label :game_id %>
    <%= form.select(:game_id, Game.all.collect{|a| [a.name, a.id]}) %>
  </div>

  <div class="field">
    <%= form.label :rules %>
    <%= form.file_field :rules %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
